import { StyleBoxDirective } from './style-box.directive';

describe('StyleBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleBoxDirective();
    expect(directive).toBeTruthy();
  });
});
